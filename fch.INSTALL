
                        Installation Guide
                        ==================

Just do a "make install" followed by "make clean". That's all.
This will install "fch" into "/usr/local/bin" so you will need root
permissions. To install into another location, do
  BINDIR=/where/ever make install
